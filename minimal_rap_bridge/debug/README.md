# Debug Utilities

This folder contains helper utilities for exploratory analysis and media generation.

- `sweep_scene_visualize.py`: multi-seed rendering sweep and summary generation
- `make_seed_videos.py`: build videos from sweep frame folders

These are optional utilities and are not called by `run_native_policy_pipeline.sh`.
